# ? The server port (unchanged)
server.port=8080

# ? Correct database name (your residency_db database)
spring.datasource.url=jdbc:postgresql://localhost:5432/epic2database
spring.datasource.username=postgres
spring.datasource.password=rosie123455455
 # (your postgres password)

# ? JPA settings ? we're managing the schema manually (no auto-create)
spring.jpa.hibernate.ddl-auto=update

# ? Optional but very helpful during development (logs every SQL query Spring executes)
spring.jpa.show-sql=true

# ? PostgreSQL dialect (your database type)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.mvc.cors.allowed-origins=http://localhost:3000